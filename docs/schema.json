{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"name": "MediaWiki configuration parameters schema.",
	"type": "object",
	"required": ["children"],
	"additionalProperties": false,
	"properties": {
		"children": {
			"type": "array",
			"items": {
				"$ref": "#/definitions/node"
			}
		},
		"date": {
			"type": "string",
			"description": "Date of the law proposal or proposition proposal.",
			"pattern": "^\\d{4}-\\d{1,2}-\\d{1,2}"
		},
		"description": {
			"type": "string",
			"description": "Description of the law proposal or proposition proposal."
		},
		"id": {
			"type": "integer",
			"description": "ID of the law proposal or proposition proposal."
		},
		"legislature": {
			"type": "integer",
			"description": "Legislature of the law proposal or proposition proposal.",
			"minimum": 1
		},
		"place": {
			"type": "string",
			"description": "Status where the law proposal or proposition proposal is."
		},
		"type": {
			"type": "string",
			"description": "Status where the law proposal or proposition proposal is.",
			"enum": ["law-project", "law-proposal"]
		},
		"url": {
			"type": ["string","null"]
		}
	},
	"definitions": {
		"node": {
			"anyOf": [{
				"type": "object",
				"required": ["type"],
				"additionalProperties": false,
				"properties": {
					"type": {
						"type": "string",
						"description": "Child type.",
						"enum": ["bill-article-reference", "code-reference", "code-part-reference", "book-reference", "law-reference", "title-reference", "chapter-reference", "section-reference", "subsection-reference", "paragraph-reference", "article-reference", "header1-reference", "header2-reference", "header3-reference", "alinea-reference", "sentence-reference", "word-reference", "incomplete-reference", "title-definition", "article-definition", "header1-definition", "header2-definition", "header3-definition", "subparagraph-definition", "alinea-definition", "sentence-definition", "mention-definition", "word-definition", "header1", "header2", "header3", "bill-article", "amendment", "edit", "quote", "law-project", "law-proposal",  "raw-content"]
					},
					"children": {
						"type": "array",
						"description": "Node children.",
						"items": {
							"$ref": "#/definitions/node"
						}
					},
					"parent": {
						"$ref": "#/definitions/node",
						"description": "Parent node. This property is never present in DuraLex exported trees, but is always present in internal treatments."
					},
					"uuid": {
						"type": "string",
						"description": "Node unique identifier. This property is not useful and generally not present in DuraLex exported trees, but is always present in internal treatments."
					},
					"id": {
						"type": "string",
						"description": "Node ID, often used in article, law, or code nodes."
					},
					"editType": {
						"type": "string",
						"description": "Edit type.",
						"enum": ["add", "delete", "rename", "replace", "ratified", "edit"]
					},
					"lawType": {
						"type": "string",
						"description": "Law type."
					},
					"lawDate": {
						"type": "string",
						"description": "Law date.",
						"pattern": "^\\d{4}-\\d{1,2}-\\d{1,2}"
					},
					"order": {
						"type": "integer",
						"description": "Order in the list.",
						"minimum": -2
					},
					"words": {
						"type": "string",
						"description": "Words, used in definitions."
					},
					"position": {
						"type": "string",
						"description": "Position of the word, used in definitions.",
						"enum": ["before", "after", "end", "beginning"]
					},
					"content": {
						"type": "string",
						"description": "Content, when the child is not recognized."
					},
					"isNew": {
						"type": "boolean",
						"description": "This node is a new article."
					}
				},
				"patternProperties": {
					"is((Un(de?)?|Duo(de)?|Ter|Quater|Quin|Sex?|Sept|Octo|Novo)(dec|v[ei]c|tr[ei]c|quadrag|quinquag|sexag|septuag|octog|nonag)ies|(Dec|V[ei]c|Tr[ei]c|Quadrag|Quinquag|Sexag|Septuag|Octog|Nonag)ies|Semel|Bis|Ter|Quater|(Quinqu|Sex|Sept|Oct|No[nv])ies)": {
						"type": "boolean"
					}
				}
			}]
		}
	}
}
